NOTES:

# stop point 2/6/11pm-ish
line 27, typeofshape-buttons.js, working on Button class, want to finish constructor

# --- TYPEOFSHAPE-BUTTONS.JS ---
- need to finish constructor for Button class in typeofshape-button.js:
checklist:
- use a switch to case through the types
- make sure it can dynamically support most things ill need

- need to create function "createShapeButton()" in typeofshape-buttons.js
checklist:
- buttonTypes.ForEach() used to optimize code
- it gotta work


# --- OTHER ---
- need to learn communication between scripts for organization
- desperately need to learn async JSON functions Promises and all that good stuff
checklist:
(AI generated list)

 - learn JavaScript fundamentals: scope, functions, objects, arrays, modules
 - JSON basics: JSON.parse / JSON.stringify, valid JSON types
 - asynchronous JS: Promises, async/await, and error handling
 - Fetch API and HTTP: fetch(), status codes, headers, Content-Type
 - script communication: events, callbacks, `postMessage`, and modules
 - DOM manipulation & events: querySelector, addEventListener, event delegation
 - debugging & testing: DevTools Network/Console, curl, Postman/HTTPie
 - backend basics (Python): create REST endpoints with Flask or FastAPI
 - CORS and security: same-origin, CORS headers, auth tokens vs cookies
 - data validation: validate inputs and responses (pydantic, schema checks)
